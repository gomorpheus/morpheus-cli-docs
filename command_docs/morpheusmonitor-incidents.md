### morpheus monitor-incidents

```
Usage: morpheus monitor-incidents [command] [options]
Commands:
	add
	close
	get
	history
	list
	mute
	mute-all
	notifications
	reopen
	stats
	unmute
	unmute-all
	update
```
